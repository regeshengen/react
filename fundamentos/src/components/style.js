import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    global: {
        backgroundColor: '#CCC',
        flexGrow: 1,
        justifyContent: "center",
        alignItems: "center"
    },
    h1: {
        fontSize: 30
    }
})